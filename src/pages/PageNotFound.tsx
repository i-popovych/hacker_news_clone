import React from 'react';

const PageNotFound = () => {
    return (
        <div style={{color: '#ab0606', fontSize: '30px', display: "flex", justifyContent: 'center', alignItems: 'center'}}>
           <span>Error 404 not found</span>
        </div>
    );
};

export default PageNotFound;